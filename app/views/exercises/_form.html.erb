<%= form_with model: @exercise do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <%= f.label :muscle_id %>
  <%= f.select :muscle_id, Exercise.muscle_ids %><br>

  <%= f.submit 'Cadastrar' %>
<% end %>