<%= form_with model: @traning_plan, method: method_patch_or_edit do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <% f.hidden_field :user_id, value: params[:user_id].to_i %>

  <%= f.submit 'Cadastrar' %>
<% end %>
